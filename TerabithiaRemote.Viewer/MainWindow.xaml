<Window x:Class="TerabithiaRemote.Viewer.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Terabithia Remote Viewer" Height="720" Width="1200">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition/>
        </Grid.RowDefinitions>

        <StackPanel Orientation="Horizontal" Margin="10" Grid.Row="0">
            <TextBlock Text="My ID:" VerticalAlignment="Center" Margin="0,0,5,0"/>
            <TextBox Name="TxtMyId" Width="80" IsReadOnly="True" Background="#EEE" Margin="0,0,15,0" VerticalContentAlignment="Center"/>
            <TextBox Name="TxtTargetId" Width="100" Margin="0,0,8,0" VerticalContentAlignment="Center" Tag="Enter ID"/>
            <Button Name="BtnConnect" Content="Connect" Width="100" Margin="0,0,8,0" Click="BtnConnect_Click"/>
            <Button Name="BtnStartStream" Content="Start Stream" Width="120" Click="BtnStartStream_Click"/>
            <TextBlock Name="TxtStatus" Margin="12,0,0,0" VerticalAlignment="Center" FontWeight="Bold"/>

        </StackPanel>

        <Border Grid.Row="1" Margin="10" BorderThickness="1" BorderBrush="Gray">
            <Image Name="ImgScreen"
                   MouseMove="ImgScreen_MouseMove"
                   MouseDown="ImgScreen_MouseDown"
                   MouseUp="ImgScreen_MouseUp"
                   Stretch="Uniform"/>
        </Border>

    </Grid>
</Window>
