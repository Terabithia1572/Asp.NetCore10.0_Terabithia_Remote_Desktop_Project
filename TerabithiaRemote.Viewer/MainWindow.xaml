<Window x:Class="TerabithiaRemote.Viewer.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Terabithia Remote Viewer" Height="720" Width="1200">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition/>
        </Grid.RowDefinitions>

        <StackPanel Orientation="Horizontal" Margin="10" Grid.Row="0">
            <Button Name="BtnConnect" Content="Connect" Width="120" Margin="0,0,8,0" Click="BtnConnect_Click"/>
            <Button Name="BtnStartStream" Content="Start Stream" Width="140" Click="BtnStartStream_Click"/>
            <TextBlock Name="TxtStatus" Margin="12,0,0,0" VerticalAlignment="Center"/>
        </StackPanel>

        <Border Grid.Row="1" Margin="10" BorderThickness="1" BorderBrush="Gray">
            <Image Name="ImgScreen"
                   MouseMove="ImgScreen_MouseMove"
                   MouseDown="ImgScreen_MouseDown"
                   MouseUp="ImgScreen_MouseUp"
                   Stretch="Uniform"/>
        </Border>

    </Grid>
</Window>
